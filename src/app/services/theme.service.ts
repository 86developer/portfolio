import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class ThemeService {
  // Dark/light theme support removed.
  // This stub remains for compatibility with any lingering imports.
}
